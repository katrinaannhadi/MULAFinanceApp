<?xml version="1.0" encoding="utf-8"?>
    <!--
      ~ MULA Financial Application INFS3634
      -->

<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/scrollView"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:layout_below="@id/toolbar"
    android:background="?windowBackgroundColor"
    android:clipToPadding="false"
    android:fillViewport="true"
    tools:showIn="@layout/show_habit">

    <LinearLayout
        style="@style/CardList"
        android:clipToPadding="false">

        <org.mula.finance.activities.habits.show.views.SubtitleCard
            android:id="@+id/subtitleCard"
            style="@style/ShowHabit.Subtitle"/>

        <org.mula.finance.activities.habits.show.views.NotesCard
            android:id="@+id/notesCard"
            style="@style/Card"
            android:gravity="center" />

        <View
            android:id="@+id/headerShadow"
            style="@style/ToolbarShadow"/>

        <org.mula.finance.activities.habits.show.views.OverviewCard
            android:id="@+id/overviewCard"
            style="@style/Card"
            android:paddingTop="12dp"/>

        <org.mula.finance.activities.habits.show.views.ScoreCard
            android:id="@+id/scoreCard"
            style="@style/Card"
            android:gravity="center"/>

        <org.mula.finance.activities.habits.show.views.BarCard
            android:id="@+id/barCard"
            style="@style/Card"
            android:gravity="center"/>

        <org.mula.finance.activities.habits.show.views.HistoryCard
            android:id="@+id/historyCard"
            style="@style/Card"
            android:gravity="center"
            android:paddingBottom="0dp"/>

        <org.mula.finance.activities.habits.show.views.StreakCard
            android:id="@+id/streakCard"
            style="@style/Card"/>

        <org.mula.finance.activities.habits.show.views.FrequencyCard
            android:id="@+id/frequencyCard"
            style="@style/Card"/>

    </LinearLayout>
</ScrollView>